package react_flight;

/**
 * Non-Next React host capability used to prove Genes' extension seam.
 *
 * Gutenberg or another React host may establish resource provenance through
 * its own compiler policy. Genes must validate the nested payload without
 * learning the host's name, lifecycle, or diagnostics.
 */
abstract HostResource<T>(js.lib.Promise<T>) {}
