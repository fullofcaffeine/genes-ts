package library_profile;

/**
 * Provides the inert entry point used to compare application and library DCE.
 *
 * The public API is intentionally never referenced from Haxe code. Default
 * classic output may therefore strip unused members, while `genes.library`
 * must retain exactly the exposed reusable surface for external JS consumers.
 */
class Main {
  static function main():Void {}
}
