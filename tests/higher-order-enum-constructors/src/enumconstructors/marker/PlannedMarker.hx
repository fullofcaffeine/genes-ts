package enumconstructors.marker;

/**
 * Type named only by a planned higher-order enum-constructor instantiation.
 *
 * The Haxe seed is an empty typed array, so it creates no runtime dependency.
 */
class PlannedMarker {}
