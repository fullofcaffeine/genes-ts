-lib genes-ts

-cp examples/todoapp/src
--main todo.web.Main

# Emit TypeScript via genes-ts (still uses -js because we compile on the JS platform)
-js tests/genes-ts/snapshot/todoapp/out/web/src-gen/index.tsx
-D genes.ts

# Bundler-friendly imports (extensionless)
-D genes.ts.no_extension

# Enable inline markup rewrite (requires @:jsx_inline_markup on the class)
-D genes.react.inline_markup

-dce full

