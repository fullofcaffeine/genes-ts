-lib genes

-cp tests_ts_minimal/src
--main my.app.Main

# Emit TypeScript via genes-ts (still uses -js because we compile on the JS platform)
-js tests_ts_minimal/src-gen/index.ts
-D genes.ts

# Opt-in: minimal runtime / no-reflection mode.
-D genes.ts.minimal_runtime

-dce full

