import {Register} from "../../genes/Register"

export type CacheQueryOptions = {
	cacheName?: string | null,
	ignoreMethod?: boolean | null,
	ignoreSearch?: boolean | null,
	ignoreVary?: boolean | null
}
