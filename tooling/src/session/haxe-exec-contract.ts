/** Maximum UTF-8 bytes accepted across Haxe environment names and values. */
export const HAXE_ENVIRONMENT_TEXT_LIMIT = 64 * 1_024;

/** Maximum serialized environment bytes the raw-exec helper will retain. */
export const HAXE_ENVIRONMENT_PAYLOAD_LIMIT = 128 * 1_024;
